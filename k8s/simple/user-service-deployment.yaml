# User Service Deployment
apiVersion: v1
kind: ConfigMap
metadata:
  name: user-service-config
  namespace: todo-app
data:
  PORT: "3002"
  NODE_ENV: "production"
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DB_NAME: "todoapp"
  DB_USER: "postgres"
---
apiVersion: v1
kind: Secret
metadata:
  name: user-service-secret
  namespace: todo-app
type: Opaque
stringData:
  DB_PASSWORD: "password"
  JWT_SECRET: "local-dev-secret-key-change-in-production"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: todo-app
  labels:
    app: user-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
        - name: user-service
          image: user-service:latest
          imagePullPolicy: IfNotPresent # For local testing with Docker Desktop
          ports:
            - containerPort: 3002
              name: http
          envFrom:
            - configMapRef:
                name: user-service-config
            - secretRef:
                name: user-service-secret
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: todo-app
  labels:
    app: user-service
spec:
  selector:
    app: user-service
  ports:
    - port: 3002
      targetPort: 3002
      protocol: TCP
  type: ClusterIP
